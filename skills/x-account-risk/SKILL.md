---
name: x-account-risk
description: 抓取 X(Twitter) 账号公开推文并通过 9 维度风险引擎(风险分逻辑，分数越高风险越大)评分，生成可视化 HTML 报告。当用户要求"评估某 X 账号风险""给 @handle 打分""分析 X 账号合规性""生成 X 账号风险评估报告"，或需要绕过 X 自动化限流抓取真实推文 DOM 时使用。
---

# X 账号抓取 + 风险评分 Skill v5.1

一键完成：**抓真实推文（登录态 DOM）→ 合并转帖分类 → 9 维度风险引擎 → 可视化 HTML 报告**。
评分逻辑：**风险分**，分数越高风险越大（≥60 高风险 / 30–59 中风险 / <30 低风险）。

## v4.1 引擎修正（2026-08-01 实战验证，与 v4 的差异）

1. **ACC 计划合规只按成人内容占比判定**：主时间线成人/敏感占比 >20% 才 +10；漏标媒体不再“双重计分”（漏标只由维度 2 负责）。
2. **Tier 1 语境化**：`萝莉/正太/cp/强迫/强奸/下药/未成年` 等词必须与严重语境共现才算 Tier 1（如“被玩具强奸了”=角色扮演、“蛋糕别下药”=自嘲梗、“未成年女友是吧”=调侃），灰色词降级 Tier 2 并提示人工复核。
3. **低互动惩罚仅适用于 ≥5000 粉账号**：小号互动低属正常现象，避免结构性误伤。
4. **短英文关键词词边界匹配**：`ts`、`cp`、`sub`、`18+` 等不再子串误匹配。

## v4.2 新增：第 10 维度「账号存续风险」（2026-08-01）

**背景**：v4/v4.1 衡量“内容合规”，但平台封禁概率还取决于账号历史与运营模式。
实测发现大量“复活号/被冻重开号”按原模式运营（性交易广告、公开联系方式），再封概率极高，却只得低分。

**第 10 维度：账号存续风险（0-15）**
- 封禁/重生史信号（复活版/重生号/被冻/冻结/重开/復活/旧号/被盗号等，仅简介+原创内容）：+4/个，上限 +8
- 性交易/商业变现信号（接线下/可约/全国可飞/莞式/报价/课表/口令/好友位/涩涩基地/包夜/线上一对一/门槛(需带数字)/付费/有偿/包月/订阅/打赏/图包/电报/加群/淘宝/店铺/发售等）：+3/个，上限 +7；“无/不+词”的声明不扣
- 隐私泄露/开盒信号（地址是/家庭住址/住址）：+5

**误报防护**：只扫描简介 + 本账号原创/回复（转帖不算）；已排除宽泛词（新号/封号/被封/线下/约吗/援交——实测“QQ被封”与“援交”自嘲梗大量误报）；“无图包/不约炮”等否定声明不扣分；“门槛”需与数字共现（门槛300），“门槛是什么”这类提问不扣分。

## v4.4 新增：第 11 维度「真人感/营销号形态」（2026-08-01）

**背景**：长期存活的成人号多为“像真人博主”的号（有人设、有生活内容、有节制），纯营销号/搬运号最容易被封与举报。

**第 11 维度：真人感/营销号形态（0-12）**
- 卖货内容占比：非转帖推文中含**露骨卖货词**（口令/课表/门槛/报价/付费/淘宝/图包/接线下等）>=25% +3、>=50% +6
- 简介含 >=2 个露骨卖货词：+3
- 生活化内容（吃饭/上课/上班/考试/朋友/天气/喝酒等强生活词）占比 <8%：+3（疑似纯营销号）
- 转帖占比 >50%：+3（搬运号）
- 真人感减免：生活化内容 >=25% -2、>=50% -4

**变现词分级（v4.5）**：
- **A 级·露骨明示（必扣）**：接线下/可约/全国可飞/莞式/报价/课表/口令/好友位/私信解锁/涩涩基地/包夜/线上一对一/接单/约炮/门槛(带数字)/付费/有偿/包月/订阅/打赏/图包/淘宝/店铺/发售/卖淫/嫖/下单 等
- **B 级·隐晦引流（不扣分，仅提示）**：电报/tg/加群/私信/找我/加我/解锁/可线下/🉑线下/购买/价格 等
- “无/不+词”的否定声明不扣；“门槛”需带数字；日常词“购买/价格”不算露骨变现（实测“拼多多买衣服”误报）

**误报防护**：生活词表只用强生活词（去掉“今天/日常/分享”等易被广告文案命中的宽泛词）；转帖内容不计入卖货/生活统计。

## v4.6 新增：幼态误判 / 仿冒生态 / 人工复核（2026-08-01）

- **幼态/未成年误判风险（第 10 维度 +4）**：账号自述“识别成未成年/像未成年/幼年时期/幼态”等（实测 @jiuxingmao 被 X 识别为未成年仍发涩涩内容）——幼态人设+性话题是平台误杀高发区，报告强制人工复核。
- **仿冒号生态计分（第 10 维度）**：搜索实测发现仿冒/近似账号 >=3 个 +2、>=6 个 +4（实测 @Sunnythreewater 7 个、@jiuxingmao 5+、@zixuanmiao 4+）。
- **Tier 1 搬运标注**：搬运（转帖）触发的 Tier 1 在详情中标注“（搬运）”，与原创内容区分责任。
- **报告“待人工复核清单”**：Tier 1 存疑条目、幼态信号、开盒信号、仿冒号列表统一汇总，避免散落各维度。

## v4.7 新增：置信度 / 校准 / 分级 / 治理（2026-08-01）

- **置信度机制（P0）**：api_reply/ip_network 等无数据维度不再假装安全——报告输出“置信度”与“分数区间（±10）”；样本覆盖率 <50% 时标注低置信度（实测 @lengmei111 覆盖率 7.8% → 置信度 42%）。
- **第一轮校准（P0）**：160 个存活同类账号画像显示，简介露骨变现组与无变现组中位存续年限均为 4 年——变现信号对“存续”预测力弱，第 10 维度变现上限 7→5，权重让给平台惩罚状态（冻结史/限流/幼态误判）。
- **Tier2 数量分级（P1）**：1-2 条 +5 / 3-9 条 +10 / 10-49 条 +15 / ≥50 条 +20，恢复擦边浓度信息。
- **marking 平台标记率参照（P1）**：平台整体标记率 <10% 时（平台认可的擦边尺度），漏标扣分按 50% 折算并注明；≥10% 正常扣。
- **增量监控/趋势（P1）**：每次评估追加 `data/history/<handle>.jsonl`，报告显示“上次→本次”分数变化。
- **回归基线（P1）**：`data/golden_set.json` 固化 15 账号 v4.7 分数，引擎改动后可自动对比漂移。
- **多语言词库（P2）**：增加英文关键词（sex/porn/hentai/onlyfans/selling content 等）。
- **数据治理（P2）**：报告标注“敏感数据等级：高，仅限授权使用，禁止公开传播”。
- **版本统一（P2）**：风险输出文件统一为 `<handle>_risk.json`（不再使用历史 _risk_v3.json 命名）。

## v4.8 第二轮校准修正（2026-08-01）

**17 个正样本验证集（validation_set_v1.json）**：5 个确认死亡号（含 @mozidoum“老号被封”）+ 12 个自述重生史账号。

**11 vs 149 特征对比推翻了 v4.7 的两处假设**：
- **注册年限**：重生史组中位 1 年 vs 无史组 4 年 → 新增“注册 <1 年且无认证 +2”（新号信号）
- **bio 露骨变现**：重生史组 55% vs 无史组 23%（+0.31）→ 变现号更容易被封，**变现权重上限恢复 5→7**（v4.7 用“存活年限无差异”得出的降权结论是口径错误）
- **封禁表述召回**：当前词表仅召回 3/11，扩展模式（大号被封/老号被封/账号被封/号被封了/秽土转生/转生）召回 6/11 且误报仅 1% → 恢复“被封”类精确模式词（避免裸词“被封”误报 QQ 封号）
- **蓝标**：18% vs 21% 无差异 → Premium -2 信任加分保留

## v4.9：train/test 校准 + 复核落盘（2026-08-01）

- **数据集扩充到 358 样本**（validation_set_v2.json）：遍历关注量最大的账号（@cdmxiaoxi 6756 关注）及其余高关注账号的 following 列表（X 每页约限 50-70 个），合并 4 个来源去重；8 死号 + 8 bio 重生史 + 5 推文级重生 = 21 正样本。
- **过拟合被数据证实**：358 样本 70/30 划分，训练集 AUC 0.821 → 测试集 AUC 0.560（calibration_train_test.json）——训练集上调的权重在测试集失效。
- **回退动作**：移除“注册<1年新号 +2”；变现权重上限回退 5（v4.8 的 7 为训练集调参）。
- **marking 折算收紧**：平台标记率 <10% 且 NSFW 占比 <30% 才打折（露骨号不享受折算，实测 @dangao 71% NSFW 恢复全额扣分）。
- **复核落盘**：存疑条目（Tier1/幼态/开盒）写入 `data/review/<handle>.json`（verdict: 实锤/角色扮演/比喻/无法判断），报告显示复核状态。

## v5.0：万级样本最终校准（2026-08-01）

- **数据集 v3：10523 账号（10516 alive + 4 dead + 121 bio 重生史 = 125 正样本）**——遍历 350 个账号的关注列表（15972 条记录 → 10554 唯一 handle），10273 个新候选批量探测完成（4 并发 + 限流保护 + 断点续跑）。
- **train/test 最终结果（calibration_final.json）**：组合模型测试集 AUC 0.819（无过拟合）；但单特征揭示真相——
  - `ban`（bio 封禁/重生表述）：唯一可靠信号（AUC 1.000，负样本误报极低；注意标签同源性，主要价值是低误报）
  - `sell_a`（露骨变现）：AUC 0.196（**方向反向**）——大样本推翻了 v4.8“变现加分”结论
  - `sell_b`：0.253（反向）、`age`：0.186（反向）、`verified`：0.124（反向，支持蓝标信任加分）
- **v5.0 引擎调整**：变现权重上限 5→3（仅弱提示）；ban 上限 8 保留；开盒 +5、幼态 +4、仿冒 +2/+4 保留（性质明确但标注启发式）；蓝标 -2 保留。
- **新增**：`scripts/regression_check.py`（golden 漂移 >3 告警）、报告“行动矩阵”（0-24 常规 / 25-39 关注 / 40-54 整改 / 55+ 立即处理）。

## v5.1：合并 GitHub master 分支（2026-08-02）

- **config.json 外部化**：关键词/阈值/校准权重全部可配置（`config.json`，引擎同目录自动加载，未配置用内置默认）。
- **分母修正**：api_reply/ip_network 两个无数据维度从总分分母移除（142→120），避免无数据维度系统性拉低分数；报告输出 `dim_coverage`（有效维度 9/11）。
- **行动矩阵阈值随分母调整**：0-20 常规 / 21-33 关注 / 34-45 整改 / ≥46 立即处理。
- **⚠️ 不采纳 master 的 `_hit()` 中文边界实现**：其 `(?<![a-z0-9\u4e00-\u9fa5])` 边界会让“男娘”在“小男娘”中漏检（中文无词边界，2 字组合词大量漏检）；保留 ASCII 短词边界 + 中文子串匹配。
- 校准权重（sell 3 / ban 8 / dox 5 / minor 4 / impersonate 2/4 / verified -2）与 config.json 一致，可通过配置调整。

## v5.1.1：血腥词误报修复（2026-08-06）
- **案例**：@dangao0709 一句“出了点意外出血了，太血腥没法发”（说自己意外受伤）被“血腥”关键词误判 Tier1 +20。
- **修复**：Tier 1 关键词移除“血腥”单独词（保留 分尸/剥皮/gore/snuff/凌迟 等无歧义词）；config.json 同步更新。
- **教训**：“血腥”是日常词（意外出血、血腥玛丽等），必须与 分尸/剥皮/凶杀 等强语境共现才能算 Tier 1。

## v5.1.2：轮奸语境自指修复（2026-08-10）
- **案例**：@chichi_maddy “清纯得像一回村就会被抓去祭奠上轮奸的土妹”——“轮奸”作为自嘲比喻被误判 Tier1 +20。
- **根因**：TIER1_RAPE_CONTEXT 共现词表里包含“轮奸”自身，导致“轮奸”单独出现就自我满足共现条件。
- **修复**：TIER1_RAPE_CONTEXT 移除“轮奸”（config.json 同步）；真正的下药/迷奸轮奸内容仍通过“下药/迷药”分支正常命中（@lengmei111 下药轮奸案例不受影响）。

## 评分引擎表（11 维度，总分 142 → 归一化 0-100）

| 序号 | 维度名称 | 原始分值 | 含义 |
|------|----------|---------|------|
| 1 | **ACC 计划合规** | 0-15 | 主时间线成人/敏感内容占比 >20% 且无 ACC 计划成员证据 +10（2026 年强制）；占比 ≤20% 计 0。 |
| 2 | **ACC 三级标记合规** | 0-15 | 成人关键词 + 媒体 + 未标记 Sensitive Media，每条 +3（上限 15）。 |
| 3 | **API 自动回复合规** | 0-12 | API v2 2026 限制：自动回复必须提及/引用原作者（未提及 +3/次）；同一推文被回复 >10 次 +2/条；回复内容重复 >5 条 +3。 |
| 4 | **IP/网络环境合规** | 0-10 | 数据中心 IP（AWS/阿里云/腾讯云/Tor）+5；频繁 IP 切换（>5 个/周）+3；住宅 IP 正常。 |
| 5 | **Shadowban 隐形限制** | 0-10 | 搜索用户名无推文显示 +6；回复深度 <3 层 +3；印响数骤降 >50% +3；特定搜索无该账号推文 +4（需实测，注意查看者敏感过滤对照组）。 |
| 6 | **关注/粉丝比与增长** | 0-8 | 关注/粉丝比 >10:1 +4；粉丝机器人占比 >30% +3；关注列表 >50% 被封 +2。 |
| 7 | **Premium 会员等级** | 0-8 | Premium Basic 0；Premium -2 信任加分；Premium+ -5；粉丝 >10K 未开通 +2（蓝标推断 Premium）。 |
| 8 | **内容多样性与活跃度** | 0-12 | 单一 NSFW 占比 >80% +5；原创/搬运比 <1:3 +4；60% 推文在 2 小时内 +3；>70% 互动低（<5 赞，仅 ≥5000 粉） +3。 |
| 9 | **禁止内容零接触** | 0-25 | Tier 1（非合意/未成年/性暴力/血腥，需语境判定）1 条 +20、≥2 条 +25；Tier 2 边界 1-2 条 +5、3-9 条 +10、10-49 条 +15、≥50 条 +20。 |
| 10 | **账号存续风险** | 0-15 | 封禁/重生史 +4/个（上限 8）；性交易/变现信号 +3/个（上限 5，校准下调）；隐私泄露/开盒 +5；幼态/未成年误判 +4；仿冒号 >=3 +2 / >=6 +4。 |
| 11 | **真人感/营销号形态** | 0-12 | 露骨卖货内容 >=25% +3 / >=50% +6；简介露骨卖货词 >=2 个 +3；生活化内容 <8% +3；搬运 >50% +3；生活化 >=25% -2 / >=50% -4；隐晦引流词不扣分仅提示。 |

等级：总分 ≥60 = 高风险（红）；30-59 = 中风险（黄）；<30 = 低风险（绿）。
详细实现见随包 `risk_engine.py` 的 `_get_dimensions_v4()`。

## 前置依赖

- **Node + Playwright**：`npm i playwright`；支持 `channel: 'msedge'/'chrome'`（用系统浏览器可免 `npx playwright install chromium`）。
- **Python 3.10+**：随包 `risk_engine.py`（核心引擎，需复制到项目目录）与 `scripts/*.py`。
- **登录态 cookie JSON**（Cookie-Editor 导出格式 `[{name,value,domain,path,expirationDate,...}]`），放 `<workspace>/conny_cookies.json`；任意有效 X 登录态即可，不需要目标账号本人的 cookie。
- **fxTwitter 公开资料** `work/fx_<handle>.json`：`curl https://api.fxtwitter.com/<handle>`，用于生成 profile（粉丝/认证/加入时间）与推文总数。

## 工作流（三步走 + 搜索实测）

### Step 1 抓取 + 合并转帖分类

```bash
cd <项目目录>
node scripts/fetch_x_tweets.js <Handle> [workspace_dir] [cookie_file]
python scripts/merge_timelines.py <Handle> [workspace_dir] [fx_json]
```

- `fetch_x_tweets.js`：启动无头 Chromium/Edge + 注入 cookie → 打开 `https://x.com/<Handle>`；**自动点击“此个人资料可能包含潜在的敏感内容”警告门**（`button[data-testid="empty_state_button_text"]`）；滚动增量去重收集（连续 5-8 次无新增停止）；自动展开被折叠的敏感媒体；逐条提取 ID/时间/点赞/转帖/阅读/媒体/敏感标记。输出 `data/<handle>_deep_main.json` 与 `data/<handle>_deep_replies.json`。
- `merge_timelines.py`：**主时间线全部文章计入本账号**（作者 != handle 即本账号的转帖）；回复标签页只保留作者 == handle 的回复；去重（主时间线优先）；输出 `data/<handle>_tweets.json` + `data/<handle>_profile.json`（由 fxTwitter 资料生成）。

### Step 2 跑风险评分

```bash
cd <项目目录>   # 必须含 risk_engine.py（复制自技能包根目录）
python scripts/assess_x_account.py <Handle> [workspace_dir]
```

- 可选搜索证据：`data/<handle>_search_evidence.json`（见 Step 4），提供 `search_autocomplete_absent` / `user_search_absent` / `from_search_empty` / `from_search_works` / `impersonators` 字段。
- 输出：`data/<handle>_risk_v3.json`（score/level/dimensions 9 维度/meta/tweets）。

### Step 3 生成 HTML 报告

```bash
python scripts/gen_report.py <workspace>/data/<handle>_risk_v3.json [output.html]
```

- v4 九维度报告：总分圆环 + 9 维度卡片（扣分标准/你的情况/改进建议）+ 关键发现（数据驱动）+ 推文样本表 + 数据覆盖说明。

### Step 4 搜索可见性实测（建议，供维度 5）

```bash
node scripts/search_visibility_test.js <Handle> [workspace_dir] [cookie_file]
node scripts/auto_retest.js <Handle> [workspace_dir] [cookie_file]
```

- 自动补全：检查是否出现“前往 @handle”直达入口；用户搜索：检查精确真号是否在结果中；`from:` 搜索：有结果 = 未被推文搜索限流。
- **对照组**：`from:` 零结果可能是查看者“隐藏敏感内容”设置所致（实测所有敏感账号 from: 均为空），必须用其他账号对照，不能直接判定 shadowban。
- 结果写入 `data/<handle>_search_evidence.json` 后重跑 Step 2。

## 重要坑位（详见 references/LESSONS.md）

1. **敏感个人资料警告门**：不点击“是，查看个人资料”，`article` 为 0，抓取看似成功实则为空。
2. **转帖识别**：“已转帖”横幅不在正文 `div[lang]` 中；主时间线里作者 != 本账号 = 本账号转帖（漏识别会导致搬运号分数严重低估）。
3. **媒体检测**：不能只查 `img[data-testid="tweetPhoto"]`（头像也是 pbs.twimg.com 图片）；用 `div[data-testid="tweetPhoto"/"tweetVideo"]` + `img[src*="/media/"]`，排除 `profile_images`。
4. **互动数字解析**：X 把回复/转帖/赞/书签/阅读放在同一个 aria-label，必须按关键词分别提取，不能取第一个数字。
5. **Tier 1 语境化**：灰色词（萝莉/正太/cp/强迫/下药/未成年/强奸）在成人圈常见于角色扮演、自嘲、标签语境，直接命中会大量误报。
6. **小号公平性**：<5000 粉账号互动低属正常，低互动惩罚仅适用大号。
7. **GraphQL API 持续 401**：页面能登录 ≠ API 能调，直接走 Playwright DOM。
8. **无限滚动丢节点**：必须增量去重收集（`Map`，key=id 或 time+text），不能最后一次性提取。
9. **中文资料页**：用「关注者/正在关注」正则，否则 followers/following 解析错乱。
10. **Cookie 有效性**：过期/被吊销会触发登录墙（脚本检测后 exit 3），需重新导出。

## 注意事项

- 临时测试账号不触发邮件通知；正式监控账号 ≥60 分才发邮件（如接 QQ 邮箱 MCP，`alias_id` 传字符串、`to` 传数组）。
- 数据落盘在 `<workspace>/data/`，报告 HTML 默认输出到同目录，可指定输出路径。
- cookie 属敏感凭据：仅本地使用，不要写入报告/交付物。
